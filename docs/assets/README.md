# Assets

Coloca aquí las 2 imágenes de portada referenciadas por el `README.md` principal:

```text
docs/assets/portada-a4.png     -> Captura del PDF A4
docs/assets/portada-ticket.png -> Captura del ticket térmico 80 mm
```

Recomendaciones:

- Formato `PNG` o `JPG`, ancho 1200 px aprox.
- Peso menor a 1 MB por imagen para no inflar el repo.
- Sin datos reales de clientes (usa el emisor demo `LOCAL`).
